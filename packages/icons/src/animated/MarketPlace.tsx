import { MarketPlace as Bold } from "../bold";
import { MarketPlace as Linear } from "../linear";
import { makeMultIcon } from "../multi-icon";

export const MarketPlace = makeMultIcon({
  bold: <Bold />,
  linear: <Linear />,
});
