import { ArrowCircleLeft as Bold } from "../bold";
import { ArrowCircleLeft as Linear } from "../linear";
import { makeMultIcon } from "../multi-icon";

export const ArrowCircleLeft = makeMultIcon({
  bold: <Bold />,
  linear: <Linear />,
});
