import { CloseSquare as Bold } from "../bold";
import { CloseSquare as Linear } from "../linear";
import { makeMultIcon } from "../multi-icon";

export const CloseSquare = makeMultIcon({
  bold: <Bold />,
  linear: <Linear />,
});
