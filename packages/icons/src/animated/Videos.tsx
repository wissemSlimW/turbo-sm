import { Videos as Bold } from "../bold";
import { Videos as Linear } from "../linear";
import { makeMultIcon } from "../multi-icon";

export const Videos = makeMultIcon({
  bold: <Bold />,
  linear: <Linear />,
});
