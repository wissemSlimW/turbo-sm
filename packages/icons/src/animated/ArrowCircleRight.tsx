import { ArrowCircleRight as Bold } from "../bold";
import { ArrowCircleRight as Linear } from "../linear";
import { makeMultIcon } from "../multi-icon";



export const ArrowCircleRight = makeMultIcon({
  bold: <Bold />,
  linear: <Linear />,
});
