import { Send2 as Bold } from '../bold'
import { Send2 as Linear } from '../linear'
import { makeMultIcon } from "../multi-icon";

export const Send2 = makeMultIcon({
  bold: <Bold />,
  linear: <Linear />,
})
