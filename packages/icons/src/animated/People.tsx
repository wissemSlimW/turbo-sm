import { People as Bold } from "../bold";
import { People as Linear } from "../linear";
import { makeMultIcon } from "../multi-icon";

export const People = makeMultIcon({
  bold: <Bold />,
  linear: <Linear />,
});
