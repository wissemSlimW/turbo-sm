import { Home as Bold } from "../bold";
import { Home as Linear } from "../linear";
import { makeMultIcon } from "../multi-icon";

export const Home = makeMultIcon({
  bold: <Bold />,
  linear: <Linear />,
});
