import { seedComments } from "./comments.seed";
import { seedPosts } from "./posts.seed";
import { seedUsers } from "./users.seed";

export const seedData = () => {
  // seedUsers();
  // const { media: postMedia, posts } = await seedPosts(users);
  // const { comments } = await seedComments({ posts, users });
  // return { users, media: [...userMedia, ...postMedia], posts, comments };
};
